apiVersion: apps/v1
kind: Deployment
metadata:
  name: tmcompetodeployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: adminapp
  template:
    metadata:
      labels:
        app: adminapp
    spec:
      containers:
      - name: my-admin-app
        image: praful33/tm_competo:0.0.1
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
# service type loadbalancer
---
apiVersion: v1
kind: Service
metadata:
  name: admin-app-svc
spec:
  selector:
    app: adminapp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer